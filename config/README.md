# Configuration

Ce dossier accueillera les exemples de configuration.

Fichiers prevus :

- `.env.example` : variables d'environnement.
- `config.example.yaml` : configuration lisible du prototype.
- `logging.example.yaml` : configuration optionnelle des logs.

Les fichiers contenant des secrets ou une configuration locale reelle ne doivent
pas etre commits.
